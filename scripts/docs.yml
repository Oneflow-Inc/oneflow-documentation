cn/docs/basics/01_quickstart.md:
  RUN: all

en/docs/basics/01_quickstart.md:
  RUN: all

cn/docs/basics/02_tensor.md:
  RUN: all 

en/docs/basics/02_tensor.md:
  RUN: all 

cn/docs/basics/03_dataset_dataloader.md:
  RUN: all

en/docs/basics/03_dataset_dataloader.md:
  RUN: all

cn/docs/basics/04_build_network.md:
  RUN: all

en/docs/basics/04_build_network.md:
  RUN: all

cn/docs/basics/05_autograd.md:
  # 第 4、9 个代码块是错误示例，所以此处不运行
  # 需要使用 0.7.0 进行测试，此后版本中 Tensor 对象没有 zero_ 属性，而第 6 个代码块使用了 zero_
  RUN: [0, 1, 2, 4, 5, 6, 7, 9, 10]

en/docs/basics/05_autograd.md:
  # 第 4、9 个代码块是错误示例，所以此处不运行
  # 需要使用 0.7.0 进行测试，此后版本中 Tensor 对象没有 zero_ 属性，而第 6 个代码块使用了 zero_
  RUN: [0, 1, 2, 4, 5, 6, 7, 9, 10]

cn/docs/basics/06_optimization.md:
  RUN: all

en/docs/basics/06_optimization.md:
  RUN: all

cn/docs/basics/07_model_load_save.md:
  RUN: all

en/docs/basics/07_model_load_save.md:
  RUN: all

cn/docs/basics/08_nn_graph.md:
  RUN: [0, 1, 2, 3, 4, 5, 7, 8, 9, 14, 15, 16]
  # 第 7 个代码块是伪代码；第 11、12、14 个代码块是演示；第 13 个代码块是错误示例
  # 需要注意的是：目前 mkcodes.py 只能提取出顶格的代码块，而不能提取整个代码块有缩进的情况，比如 "??? code" 和 "!!! Note" 下的代码块。对于此情况，需要将整个代码块选中，按住 Shift+Tab 取消缩进。

en/docs/basics/08_nn_graph.md:
  RUN: [0, 1, 2, 3, 4, 5, 7, 8, 9, 14, 15, 16]
  # 第 7 个代码块是伪代码；第 11、12、14 个代码块是演示；第 13 个代码块是错误示例
  # 需要注意的是：目前 mkcodes.py 只能提取出顶格的代码块，而不能提取整个代码块有缩进的情况，比如 "??? code" 和 "!!! Note" 下的代码块。对于此情况，需要将整个代码块选中，按住 Shift+Tab 取消缩进。
