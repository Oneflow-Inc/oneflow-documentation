# 格式：
# section （方括号内的内容）为相对于 oneflow-documentation 的文档路径
# Run 为需要进行运行测试的代码块，将自动捕获所有 ```text ``` 格式内的文本块。如果所有的代码块都需要运行，可以写 all 。如果出现 RuntimeError 则会报错
# Test 为需要进行测试的代码块，将自动捕获所有 ```text ``` 格式内的文本块。如果文本块与输出结果不一致则会报错（此功能正在开发中）

[README.md]

[en/docs/index.md]

[en/docs/basics/01_quickstart.md]
Run = all 
Test = 2, 3

